#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x, y;
    cin >> x;

    if (x >= 0) {
        y = exp(-x) * tan(1 / (x * x + 8.2));
    }
    else {
        y = (0.32 * x + 2.5) / (x * x + 3);
    }

    cout << y;
    return 0;
}
