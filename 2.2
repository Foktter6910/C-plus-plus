#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x, y;
    cin >> x >> y;
    if (abs(x) <= 1 && abs(y) <= 1 && (abs(x) + abs(y) <= 1)) {
        cout << "Yes";
    }
    else {
        cout << "No";
    }
    return 0;
}
